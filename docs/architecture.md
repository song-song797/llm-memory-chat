# LLM Memory Chat — 逻辑架构图

> 生成日期：2026-05-23

---

## 1. 系统全局逻辑架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户浏览器 (Browser)                            │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    React 19 SPA (TypeScript / Vite)                   │  │
│  │                                                                       │  │
│  │  ┌─────────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────────┐  │  │
│  │  │   登录/注册   │ │   聊天主界面   │ │   API 探索器   │ │  设置中心   │  │  │
│  │  │  SignUpScreen│ │  ChatWindow  │ │APIExplorerPage│ │SettingsCenter│  │  │
│  │  └─────────────┘ └──────────────┘ └──────────────┘ └─────────────┘  │  │
│  │         │               │                                │           │  │
│  │  ┌──────┴───────────────┴────────────────────────────────┴──────┐    │  │
│  │  │              api.ts — 统一 API 调用层 (HTTP / SSE)            │    │  │
│  │  └──────────────────────────┬───────────────────────────────────┘    │  │
│  └─────────────────────────────┼───────────────────────────────────────┘  │
└────────────────────────────────┼──────────────────────────────────────────┘
                                 │ HTTP / SSE (Bearer Token)
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      FastAPI 后端 (:8000) — Python 3.13                     │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        API 路由层 (routers/)                          │  │
│  │                                                                      │  │
│  │  ┌──────┐ ┌──────┐ ┌──────────┐ ┌───────┐ ┌──────────┐ ┌────────┐ │  │
│  │  │ auth │ │ chat │ │ chat_v1  │ │ conv  │ │ memories │ │attachs │ │  │
│  │  │ 认证  │ │ 聊天  │ │V1兼容接口 │ │ 对话  │ │  记忆    │ │ 附件   │ │  │
│  │  └──┬───┘ └──┬───┘ └────┬─────┘ └──┬────┘ └────┬─────┘ └───┬────┘ │  │
│  │     │        │          │          │           │            │       │  │
│  │  ┌──┴───┐ ┌──┴──┐ ┌────┴───┐                                                 │  │
│  │  │audit │ │debug│ │projects│   ┌─────────────────────────────────┐   │  │
│  │  │审计  │ │调试  │ │ 项目   │   │    memory-candidates / docs     │   │  │
│  │  └──────┘ └─────┘ └────────┘   │    记忆候选 / 记忆文档           │   │  │
│  │                                  └─────────────────────────────────┘   │  │
│  └────────────────────────────┬─────────────────────────────────────────┘  │
│                               │                                             │
│  ┌────────────────────────────┴─────────────────────────────────────────┐  │
│  │                        业务逻辑层 (services/)                         │  │
│  │                                                                      │  │
│  │  ┌───────────────────────── 核心服务 ──────────────────────────────┐ │  │
│  │  │                                                                 │ │  │
│  │  │  ┌─────────────┐    ┌──────────────┐    ┌──────────────────┐  │ │  │
│  │  │  │  llm_service │    │ auth_service │    │ attachment_svc   │  │ │  │
│  │  │  │ LLM 调用/流式 │    │ 密码/令牌/会话│    │ 文件上传/下载    │  │ │  │
│  │  │  └──────┬──────┘    └──────────────┘    └──────────────────┘  │ │  │
│  │  │         │                                                      │ │  │
│  │  └─────────┼──────────────────────────────────────────────────────┘ │  │
│  │            │                                                        │  │
│  │  ┌─────────┴─────── 记忆子系统 (Memory System) ──────────────────┐ │  │
│  │  │                                                               │ │  │
│  │  │  ┌──────────────┐  ┌───────────────────┐  ┌───────────────┐ │ │  │
│  │  │  │ memory_svc   │  │ memory_extraction │  │ memory_cand   │ │ │  │
│  │  │  │ 记忆 CRUD    │  │ LLM 自动提取       │  │ 候选生命周期   │ │ │  │
│  │  │  │ 上下文注入    │  │ 敏感信息过滤       │  │ 去重/自动接受  │ │ │  │
│  │  │  └──────┬───────┘  └───────────────────┘  └───────────────┘ │ │  │
│  │  │         │                                             │       │ │  │
│  │  │  ┌──────┴──────────┐  ┌───────────────────┐          │       │ │  │
│  │  │  │ memory_document │  │ conv_memory_svc   │          │       │ │  │
│  │  │  │ 记忆文档聚合     │  │ 对话记忆压缩       │          │       │ │  │
│  │  │  └─────────────────┘  └───────────────────┘          │       │ │  │
│  │  │                                                       │       │ │  │
│  │  │  ┌───────────────────┐  ┌────────────────────────────┘       │ │  │
│  │  │  │ memory_history    │  │ memory_audit                       │ │  │
│  │  │  │ 版本追踪          │  │ 操作审计日志                        │ │  │
│  │  │  └───────────────────┘  └────────────────────────────────────┘ │  │
│  │  │                                                               │ │  │
│  │  │  ┌─────────────────── 向量检索子系统 ──────────────────────┐  │ │  │
│  │  │  │                                                        │  │ │  │
│  │  │  │  ┌──────────────┐  ┌──────────────────┐  ┌──────────┐ │  │ │  │
│  │  │  │  │ embedding_svc│  │ memory_embed_svc │  │ vector   │ │  │ │  │
│  │  │  │  │ 向量生成      │  │ 嵌入生命周期管理  │  │ _search  │ │  │ │  │
│  │  │  │  └──────────────┘  └──────────────────┘  │ 语义检索  │ │  │ │  │
│  │  │  │                                             └──────────┘ │  │ │  │
│  │  │  └────────────────────────────────────────────────────────┘  │ │  │
│  │  └──────────────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                               │                                          │
│  ┌────────────────────────────┴────────────────────────────────────────┐  │
│  │                      数据访问层 (SQLAlchemy ORM)                      │  │
│  │                                                                     │  │
│  │  models.py — 12 张表 ORM 映射    database.py — 引擎/会话管理         │  │
│  │  schemas.py — Pydantic 请求/响应模型                                  │  │
│  └────────────────────────────────────┬────────────────────────────────┘  │
└───────────────────────────────────────┼──────────────────────────────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    ▼                                       ▼
        ┌───────────────────┐                 ┌───────────────────────┐
        │   SQLite (开发)    │                 │ PostgreSQL + pgvector  │
        │   本地文件存储      │                 │ 生产环境 / 向量检索     │
        └───────────────────┘                 └───────────────────────┘

                    ┌───────────────────────────────────────┐
                    │         外部 LLM 服务 (OpenAI 兼容)     │
                    │                                       │
                    │  ┌───────┐ ┌──────┐ ┌──────┐ ┌─────┐ │
                    │  │ Qwen  │ │ Kimi │ │MiniMax│ │GLM  │ │
                    │  └───────┘ └──────┘ └──────┘ └─────┘ │
                    └───────────────────────────────────────┘
```

---

## 2. 记忆系统数据流架构

```
                         用户发送消息
                              │
                              ▼
                    ┌───────────────────┐
                    │   chat router     │
                    │   (SSE 流式响应)   │
                    └─────────┬─────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐
    │ 记忆上下文注入 │ │  LLM 对话    │ │ 异步：记忆候选提取  │
    │ memory_svc   │ │ llm_service  │ │ memory_extraction │
    │ .build_ctx   │ │ .stream_call │ │ .extract          │
    └──────────────┘ └──────────────┘ └────────┬─────────┘
              │               │                 │
              │               │                 ▼
              │               │      ┌──────────────────┐
              │               │      │ 敏感信息过滤       │
              │               │      │ 去重/更新检测      │
              │               │      └────────┬─────────┘
              │               │               │
              │               │         ┌─────┴──────┐
              │               │         ▼            ▼
              │               │   高置信度候选   普通候选
              │               │   (inline展示)  (设置面板)
              │               │         │            │
              │               │         ▼            ▼
              │               │   ┌──────────┐ ┌──────────────┐
              │               │   │ 内联候选   │ │memory_cand   │
              │               │   │ 推送前端   │ │ 待用户审核    │
              │               │   └──────────┘ └──────────────┘
              │               │
              ▼               ▼
        ┌───────────────────────────┐
        │     SSE 流式响应返回       │
        │  content + conversation_id │
        │  + memory_info             │
        └───────────────────────────┘


    ══════════════════════════════════════════════════════════

    记忆上下文注入详细流程 (memory_svc.build_context):

    用户查询 ──▶ 检查记忆文档 ──┬── 有文档 ──▶ 使用文档内容
                                │
                                └── 无文档 ──▶ 加载独立记忆条目
                                                    │
                                            ┌───────┴───────┐
                                            ▼               ▼
                                    pgvector 可用      仅 SQLite
                                            │               │
                                            ▼               ▼
                                    语义相似度检索      按时间排序
                                    (cosine Top-K)
                                            │               │
                                            └───────┬───────┘
                                                    ▼
                                        组装系统提示消息
                                    "以下是关于当前用户的长期记忆..."
                                                    │
                                                    ▼
                                            注入对话上下文
```

---

## 3. 记忆生命周期状态机

```
                                    LLM 自动提取
                                         │
                                         ▼
                                  ┌──────────────┐
                                  │   candidate   │
                                  │   (候选状态)   │
                                  └──────┬───────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    ▼                    ▼                    ▼
             ┌──────────┐        ┌──────────┐         ┌──────────┐
             │  accepted │        │ deferred │         │ dismissed│
             │  (已接受)  │        │ (已推迟)  │         │ (已忽略)  │
             └─────┬────┘        └─────┬────┘         └──────────┘
                   │                    │
                   ▼                    │  再次评估
           ┌──────────┐                │
           │  memory   │◀──────────────┘
           │ (正式记忆) │
           └─────┬────┘
                 │
        ┌────────┼────────┐
        ▼        ▼        ▼
   ┌────────┐┌────────┐┌────────┐
   │ global ││project ││  conv  │
   │ 全局    ││ 项目   ││ 对话   │
   │ ≤4条   ││ ≤6条   ││ ≤6条   │
   └────────┘└────────┘└───┬────┘
                           │
                    超过阈值触发
                           │
                           ▼
                   ┌──────────────┐
                   │   compaction │
                   │  (记忆压缩)   │
                   │  LLM 生成摘要 │
                   └──────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │  archived    │
                   │ (已归档)      │
                   └──────────────┘

    记忆同时触发:
    ┌─────────────────┐  ┌───────────────────┐  ┌──────────────────┐
    │ memory_history  │  │ memory_audit_log  │  │ memory_document  │
    │ 版本变更记录     │  │ 操作审计日志       │  │ 后台聚合文档      │
    └─────────────────┘  └───────────────────┘  └──────────────────┘
```

---

## 4. 数据库实体关系

```
┌──────────┐     ┌───────────────┐     ┌──────────────┐
│  users   │────▶│ user_sessions │     │  projects    │
│          │ 1:N │               │     │              │
└────┬─────┘     └───────────────┘     └──────┬───────┘
     │                                        │
     │ 1:N                               1:N  │
     ▼                                        ▼
┌──────────────┐◀────────────────────────┐
│conversations │                         │
│              │─────────────────────────┘
└──────┬───────┘
       │
  1:N  │
       ▼
┌──────────────┐     ┌──────────────┐
│  messages    │────▶│ attachments  │
│  (含版本链)   │ 1:N │              │
└──────────────┘     └──────────────┘


    记忆系统表关系:

┌──────────────┐          ┌──────────────────┐
│   memories   │◀─────────│ memory_candidates │
│              │  转化自    │                  │
│ scope:       │          └──────────────────┘
│  global      │
│  project     │──────┐   ┌──────────────────┐
│  conversation│      │   │ memory_documents │
└──────┬───────┘      └──▶│ (按 scope 聚合)  │
       │                  └──────────────────┘
       │ 1:N
       ▼
┌──────────────────┐
│ memory_embeddings│
│ pgvector 1536维  │
└──────────────────┘

┌──────────────┐     ┌───────────────────┐
│memory_histories│    │ memory_audit_logs │
│ 版本变更追踪   │    │ 操作审计记录       │
└──────────────┘     └───────────────────┘
```

---

## 5. 前端组件架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        App.tsx (全局状态 + 路由)                  │
│                                                                 │
│  State: currentUser / conversations / messages / memories /     │
│         model / theme / settings / sidebar                      │
│                                                                 │
│  Persist: localStorage (memory-chat:*)                          │
│                                                                 │
│  Routes:                                                        │
│    / ──────────▶ 主界面                                         │
│    /api-explorer ▶ API 探索器                                   │
├────────────┬────────────────────────────────────────────────────┤
│            │                                                    │
│  ┌─────────┴──────────┐  ┌──────────────────────────────────┐ │
│  │   未认证入口        │  │        已认证主界面               │ │
│  │  SignUpScreen       │  │                                  │ │
│  │  (登录/注册/        │  │  ┌──────────┐ ┌───────────────┐ │ │
│  │   落地页 Agent)     │  │  │ Sidebar  │ │ ChatWindow    │ │ │
│  │                     │  │  │          │ │               │ │ │
│  └────────────────────┘  │  │ ·项目列表 │ │ ·消息流       │ │ │
│                          │  │ ·对话列表 │ │ ·内联记忆候选 │ │ │
│                          │  │ ·新建对话 │ │ ·代码高亮     │ │ │
│                          │  └──────────┘ │ ·数学公式渲染  │ │ │
│                          │               │ ┌───────────┐  │ │ │
│                          │               │ │ ChatInput │  │ │ │
│                          │               │ │ ·文本输入  │  │ │ │
│                          │               │ │ ·附件上传  │  │ │ │
│                          │               │ │ ·模型选择  │  │ │ │
│                          │               │ └───────────┘  │ │ │
│                          │               └───────────────┘ │ │
│                          │                                  │ │
│                          │  ┌────────────────────────────┐ │ │
│                          │  │      设置面板 (Drawer)      │ │ │
│                          │  │                            │ │ │
│                          │  │  GeneralSettingsPanel      │ │ │
│                          │  │  MemorySettingsPanel       │ │ │
│                          │  │  MemoryDocumentSection     │ │ │
│                          │  │  DataSettingsPanel         │ │ │
│                          │  │  AccountSettingsPanel      │ │ │
│                          │  └────────────────────────────┘ │ │
│                          └──────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  API Explorer (/api-explorer)             │  │
│  │                                                          │  │
│  │  ActionBar │ ParamsPanel │ MessagesEditor │ DocsTab      │  │
│  │  DebugResultsTab │ HistoryTab │ SDKExamplesTab           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. API 接口架构

```
┌────────────────────────────────────────────────────────────────────┐
│                         API 端点总览                                │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  🔐 认证模块 /api/auth                                             │
│  ├── POST   /register          注册                                │
│  ├── POST   /login             登录                                │
│  ├── GET    /me                获取当前用户                         │
│  └── POST   /logout            登出                                │
│                                                                    │
│  💬 聊天模块 /api                                                  │
│  ├── POST   /chat              主聊天 (SSE流式, 需认证)             │
│  ├── POST   /landing-chat      落地页聊天 (无需认证)                │
│  └── GET    /models            可用模型列表                         │
│                                                                    │
│  📡 V1 兼容模块 /api/v1 (可选认证)                                  │
│  ├── POST   /chat              自定义记忆注入                       │
│  ├── POST   /chat/auto         自动记忆注入                        │
│  └── POST   /chat/simple       无记忆纯对话                        │
│                                                                    │
│  📋 对话管理 /api/conversations                                    │
│  ├── GET    /                  对话列表                            │
│  ├── POST   /                  创建对话                            │
│  ├── GET    /:id               对话详情 + 消息                     │
│  ├── PUT    /:id               更新对话                            │
│  ├── DELETE /:id               删除对话                            │
│  ├── POST   /:id/regenerate    重新生成回复                        │
│  └── GET    /:id/messages/versions  消息版本                       │
│                                                                    │
│  🧠 记忆管理 /api/memories                                         │
│  ├── GET    /                  按scope查询记忆                     │
│  ├── POST   /                  创建记忆                            │
│  ├── PUT    /:id               更新记忆                            │
│  ├── DELETE /:id               删除记忆                            │
│  └── POST   /refresh-documents  刷新记忆文档                       │
│                                                                    │
│  🎯 记忆候选 /api/memory-candidates                                │
│  ├── GET    /                  候选列表                            │
│  ├── POST   /:id/accept        接受候选                            │
│  ├── POST   /:id/dismiss       忽略候选                            │
│  └── POST   /:id/defer         推迟候选                            │
│                                                                    │
│  📄 记忆文档 /api/memory-documents (只读)                           │
│  └── GET    /                  文档列表                            │
│                                                                    │
│  📁 项目管理 /api/projects                                         │
│  ├── GET    /                  项目列表                            │
│  ├── POST   /                  创建项目                            │
│  ├── PUT    /:id               更新项目                            │
│  ├── DELETE /:id               删除项目                            │
│  └── PUT    /:id/archive       归档项目                            │
│                                                                    │
│  📎 附件 /api/attachments                                         │
│  └── GET    /:id/content        下载附件内容                       │
│                                                                    │
│  📊 审计 /api                                                      │
│  ├── GET    /memories/:id/history  记忆版本历史                    │
│  └── GET    /audit-logs            操作审计日志                    │
│                                                                    │
│  🔧 调试 /api/debug, /api/debug-history                           │
│  ├── POST   /debug             原始 LLM API 调试                  │
│  └── GET    /debug-history     调试历史                            │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 7. 认证与安全架构

```
┌────────────────────────────────────────────────────┐
│                   认证流程                          │
│                                                    │
│  注册: POST /register                              │
│  ┌──────────┐    ┌────────────┐    ┌───────────┐  │
│  │ 用户提交  │───▶│ scrypt 哈希 │───▶│ 创建用户  │  │
│  │ email+paw│    │ +16B salt  │    │ +session  │  │
│  └──────────┘    └────────────┘    └───────────┘  │
│                                                    │
│  登录: POST /login                                 │
│  ┌──────────┐    ┌────────────┐    ┌───────────┐  │
│  │ 用户提交  │───▶│ 验证密码   │───▶│ 生成token │  │
│  │ email+paw│    │ scrypt对比  │    │ SHA256存储 │  │
│  └──────────┘    └────────────┘    └───────────┘  │
│                                                    │
│  请求认证: Authorization: Bearer <token>            │
│  ┌──────────┐    ┌────────────┐    ┌───────────┐  │
│  │ 携带token│───▶│ SHA256哈希 │───▶│ 查询会话  │  │
│  │ 请求     │    │ 匹配       │    │ 返回用户  │  │
│  └──────────┘    └────────────┘    └───────────┘  │
│                                                    │
│  数据隔离: 所有查询强制 WHERE user_id = ?           │
│                                                    │
│  文件安全: ≤6 附件/消息, ≤10MB/文件, 随机文件名     │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## 8. 技术栈全景

```
┌─────────────────────────────────────────────────────────┐
│                       技术栈                             │
├────────────────────┬────────────────────────────────────┤
│                    │                                    │
│  前端              │  React 19 + TypeScript 6           │
│                    │  Vite 8 (构建)                     │
│                    │  Ant Design 6 (UI)                 │
│                    │  react-router-dom 7 (路由)          │
│                    │  react-markdown + rehype-katex      │
│                    │  shiki (代码高亮)                   │
│                    │  KaTeX (数学公式)                   │
│                    │                                    │
├────────────────────┼────────────────────────────────────┤
│                    │                                    │
│  后端              │  Python 3.13                       │
│                    │  FastAPI 0.115 + uvicorn 0.30      │
│                    │  SQLAlchemy 2.0 + Alembic          │
│                    │  pydantic-settings                 │
│                    │  openai SDK (兼容接口)              │
│                    │                                    │
├────────────────────┼────────────────────────────────────┤
│                    │                                    │
│  数据库            │  SQLite (开发)                      │
│                    │  PostgreSQL + pgvector (生产)       │
│                    │                                    │
├────────────────────┼────────────────────────────────────┤
│                    │                                    │
│  外部服务          │  OpenAI 兼容 LLM API               │
│                    │  (Qwen / Kimi / MiniMax / GLM)     │
│                    │  向量嵌入 API                       │
│                    │                                    │
└────────────────────┴────────────────────────────────────┘
```
